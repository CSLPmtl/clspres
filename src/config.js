/**
 * Global Settings
 */

// The API url used by the entire site.
// It's declared as the default axios URL in ./components/app.js
// it should point to an up-to-date wp-json base url, and not any route
// This is so that using v1, v2 and other custom routes needn't be a chore
export const api = 'http://localhost:8888/resources/wp-json/';
